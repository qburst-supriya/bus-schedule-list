// import { ActionType, DataType } from './types';

// const initialState: DataType<T> = {
//   data: T,
// };

// const Reducer = (state: DataType<T>, action: ActionType): DataType<T> => {
//   switch (action.type) {
//     default:
//       return state;
//   }
// };

// export default Reducer;
